import React from "react";
import Layout from "../Components/Layout";

const Extensions = () => {
  return (
    <Layout>
      <div className="page-section">Extensions</div>
    </Layout>
  );
};

export default Extensions;
